PAWZONE – FINAL MOBILE & TABLET UI SCALING

DO NOT redesign any page.

DO NOT change:
• Colors
• Layout
• Workflow
• Backend
• Components
• Desktop UI

The current design is good. The only problem is that every component is too large for mobile and tablet screens.

I want the EXACT SAME DESIGN but properly scaled for phones and tablets.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GLOBAL REQUIREMENT

Perform a complete responsive audit of every Buyer, Seller, Transporter and Admin page.

Reduce the overall scale of the mobile and tablet UI by approximately 15–25% while keeping desktop exactly unchanged.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Reduce globally:

• Banner height
• Card height
• Card padding
• Card margins
• Font sizes
• Icons
• Buttons
• Statistics cards
• Images
• Section spacing
• Widget spacing
• Input fields
• Dropdown height
• Table spacing
• Popup padding
• Navigation height
• Badge size

Everything should become visually compact while remaining comfortable to touch.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RESPONSIVE RULES

Instead of resizing individual pages manually, create a proper responsive scaling system.

Use:

• rem
• em
• clamp()
• %
• vw
• vh
• Flex
• Grid

Avoid fixed pixel dimensions wherever possible.

Desktop must remain exactly as it is today.

Only Phone and Tablet layouts should scale automatically.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MOBILE EXPERIENCE

The application should look like it was designed for phones from the beginning.

No oversized:

• headings
• cards
• banners
• statistics
• buttons
• images

Allow users to see significantly more content on the first screen without scrolling.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CARD SPACING

Reduce unnecessary white space.

Cards should appear tighter and cleaner.

Maintain equal spacing everywhere.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IMAGES

Reduce image heights slightly.

Maintain aspect ratio.

Never crop incorrectly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BUTTONS

Reduce button height.

Reduce icon size.

Reduce border radius slightly.

Keep touch targets accessible.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

HEADER

Reduce logo size.

Reduce page title size.

Reduce menu icon.

Reduce header height.

Everything should remain perfectly aligned.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BACK BUTTON

Buyer

Seller

Transporter

Change the Back button background to exactly the same green used in that page's header/banner.

Use:

• White arrow
• Same green shade as the banner
• Rounded-square design

Admin

Keep the existing Back button exactly as it is.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

QUALITY CHECK

Review EVERY Buyer, Seller, Transporter and Admin page individually.

Ensure:

✔ No oversized widgets
✔ No oversized fonts
✔ No oversized images
✔ No oversized buttons
✔ No horizontal scrolling
✔ No text clipping
✔ No overlapping
✔ Consistent spacing
✔ Perfect responsiveness on phones and tablets

The desktop version must remain completely unchanged.

The final mobile and tablet UI should look like a professionally designed native Android application while preserving the current PawZone design.