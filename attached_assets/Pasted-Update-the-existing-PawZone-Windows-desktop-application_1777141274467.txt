Update the existing PawZone Windows desktop application and fix the following logic and data issues properly.

This is a CRITICAL FIX for order flow timing and transporter route persistence.

---

# 🌙 1. NIGHT APPROVAL LOGIC CORRECTION

---

## ❌ Current Problem:

System restricts actions after 9 PM incorrectly

---

## ✅ Required Behavior:

---

### ✔ Seller

* Seller CAN:

  * View orders
  * Approve orders
  * Reject orders

AT ANY TIME (including after 9 PM)

---

### ✔ Transporter

* Transporter CAN:

  * View orders
  * Accept orders
  * Reject orders

AT ANY TIME (including after 9 PM)

---

## ⏱ Only Restriction:

```text
Timer countdown begins at 9:00 AM if order was placed after 9:00 PM
```

---

## 🧠 Correct Logic:

```js
if (orderPlacedTime >= 21:00) {
    timerStart = nextDay 09:00
} else {
    timerStart = orderPlacedTime
}
```

---

## ❗ IMPORTANT:

* DO NOT block UI actions at night
* ONLY adjust timer logic
* Users must not feel restricted

---

# 🚚 2. TRANSPORTER ROUTE NOT SAVING (CRITICAL BUG)

---

## ❌ Current Issue:

* Routes are not being saved
* Adding routes multiple times has no effect
* Data not persisting

---

## ✅ Required Fix

---

## 🔹 A. Proper Data Model

Each route must store:

* transporter_id
* start_location
* stops (array)
* end_location
* start_time
* end_time

---

## 🔹 B. Save Logic Fix

When transporter clicks “Save Route”:

* Validate all fields
* Store route in:

  * Database OR
  * Persistent local storage

---

## 🔹 C. State Update

Immediately after saving:

* Update UI
* Show newly added route

---

## 🔹 D. Fetch Logic

On page load:

* Load all routes for that transporter:

```js
routes.filter(route => route.transporter_id === currentUser.id)
```

---

## 🔹 E. Debug Protection

Add logging:

* On save → confirm success
* On fetch → confirm data exists

---

## 🔹 F. Prevent Silent Failures

If save fails:

* Show error message:
  “Route could not be saved. Please try again.”

---

## 🔹 G. UI Confirmation

After saving:

* Show success message:
  “Route added successfully”

---

# 🚚 3. ROUTE UI FIX (IMPORTANT)

---

## Ensure:

* Routes display clearly
* Show:

  * Start
  * Stops
  * End
  * Time

---

## Stops:

* Unlimited stops allowed
* Add Stop button must work
* Stops must persist after save

---

# 🧪 4. TEST CASES (MANDATORY)

---

## Night Logic

* Place order at 10 PM
* Seller approves immediately → should work
* Timer starts next day 9 AM

---

## Transporter

* Add route → appears immediately
* Refresh app → route still exists
* Add multiple routes → all visible

---

## Failure Case

* Try saving incomplete route → show error
* Try saving valid route → must succeed

---

# 🎯 FINAL OUTPUT REQUIREMENT

---

After this fix:

* Seller and transporter can act anytime (day/night)
* Timer logic works correctly
* Transporter routes save and persist properly
* No silent failures
* UI reflects real data instantly

---

Do NOT partially fix this.

This must be implemented as a proper data + logic correction.
